[project]
name = "posenc"
version = "0.1.0"
description = "Research harness for comparing positional encoding mechanisms"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26.0",
]

[project.optional-dependencies]
notebooks = [
    "ipykernel>=7.2.0",
    "matplotlib>=3.10.8",
]
